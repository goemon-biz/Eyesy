# 開発への参加

不具合報告、表示が合わないサイトの報告、使い方の説明の改善を歓迎します。

## 不具合を報告する

このリポジトリの **Issues** に、次の情報を添えてください。

- 利用している OS と Chrome のバージョン
- Eyesy のバージョン、表示モード、関係する調整値
- 再現手順と、期待した表示・実際の表示
- 公開できる範囲のサイト URL やスクリーンショット

スクリーンショットや URL に含まれる個人情報、認証用の文字列、非公開の内容は伏せてください。

## 手元で変更する

1. このリポジトリを Fork またはダウンロードします。
2. [README の手順](README.md#インストール)で、フォルダを Chrome に読み込みます。
3. ソースコードを編集します。
4. `chrome://extensions/` で Eyesy を再読み込みし、確認するページも再読み込みします。

設定画面は `popup.html`・`popup.css`・`popup.js`、ページの色変換は `content.js`、初期設定とショートカット処理は `background.js` にあります。

## 変更に応じた確認

- Dark・Filter・Normal を切り替え、全体のオン・オフが反映されること。
- 全体設定とサイト別設定が反映され、ページの再読み込み後も保存されていること。
- 通常のサイトで設定でき、Chrome の内部ページなどでは対象外になること。
- 説明文やプライバシーに関係する変更は、README・PRIVACY.md と一致すること。

JavaScript の構文を確認する場合は、Node.js が入った環境で次を実行できます。

```sh
node --check background.js
node --check content.js
node --check popup.js
```

変更を送る際は、Pull Request に変更の目的と確認した内容を書いてください。コードや素材を追加する場合は、その利用条件と必要な著作権表示も確認してください。
